services:
  db:
    image: postgis/postgis:16-3.4
    container_name: geopoly-db
    environment:
      POSTGRES_DB: polgon-db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: